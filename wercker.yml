box: wercker/nodejs
build:
  steps:
    - script:
        name: echo nodejs information
        code: |
          echo "node version $(node -v) running"
          echo "npm version $(npm -v) running"
    - script:
        name: homepage is our souce
        code: |
          export WERCKER_SOURCE_DIR='$WERCKER_SOURCE_DIR/homepage'
    - npm-install
